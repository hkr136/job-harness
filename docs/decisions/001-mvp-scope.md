# 001 — Safe first adapter scope

The initial read/search scope has expanded into adapters for all configured sources. Any irreversible flow remains adapter-specific: only Kwork has a guarded submission path, and it requires visible field checks plus exact site confirmation. Development tests remain dry-run only.
